다차원 배열의 초기화는 {} 를 겹치는 방법으로 합니다.

EX )

int ma[2][3] = {{1,2,3},{4,5,6}};
int ma[][3] = {{1,2,3},{4,5,6}};
int ma[2][3] = {1,2,3,4,5,6}; // 반드시 중첩할 필요는 없으므로 이렇게도 가능

int ma[][3] = {{1,2},{4}}; //이렇게도 초기화가 가능한데, 초기화 요소에 없는 요소는 0으로 초기화
int ma[][3] = {{1,2,0},{4,0,0}};// 같은 뜻이 도니다 

