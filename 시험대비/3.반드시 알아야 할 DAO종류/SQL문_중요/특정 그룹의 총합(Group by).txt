1번 에러 이유 : 그룹으로 묶을 만한 함수가 없다. + 특정 값들 지정 안되어있음 avg, sum등등 아니면 그냥 값들.

syear , sclass로 묶는다 해도 그 뒤의 값들은 각각의 고유한 객체 상태이므로 묶을 수가 없다.

ex)1학년 1반 으로 묶을시 그룹화된 하나의 값으로 나타나야하는데
	1학년 1반엔 학생1,학생2....등등 있으며 성적도 각각 다 다르다.

만약에 이들을 묶어야한다면 select 문에 있는 출력할 내용 전체를 group by 함수에 넣어야한다.

결국 그냥 출력하는것이다.

2번 성공 이유 : 특정그룹으로 묶음 +  특정 값들이 그룹화되어 출력 됨(합, 평균등의 값)


ex) 1학년 1반 김교사 밑으로 각각의학생들이 있다. 위에 1번 에러처럼 생각하면 이건 당연히 에러가 나야하는건데?

라고 생각이 들 수있다.

그러나 자세히 본다면 쿼리문 내에 학생을 각각 구별하는것이 아니라 그들의 반 속성(점수)의 총합, 평균을 구하는것이
기 때문에 이것들을 하나로 묶을 수 있다.